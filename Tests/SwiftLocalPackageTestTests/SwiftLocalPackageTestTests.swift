    import XCTest
    @testable import SwiftLocalPackageTest

    final class SwiftLocalPackageTestTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwiftLocalPackageTest().text, "Hello, World!")
        }
    }
